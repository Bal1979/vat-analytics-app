name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Installér afhængigheder
        run: pip install -r requirements.txt -r requirements-dev.txt
      - name: Kør tests
        run: python -m pytest -q
      - name: Validér regelkatalog (gated)
        run: python tools/build_rules_catalog.py
      - name: Uafhængig valideringssuite (gated)
        run: python -m validation.run_validation

  audit:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Installér pip-audit
        run: pip install -r requirements-dev.txt
      - name: Sårbarhedsscan af runtime-afhængigheder
        run: pip-audit --strict -r requirements.txt
